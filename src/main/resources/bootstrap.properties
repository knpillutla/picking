spring.application.name=picking
# This is the default:
spring.cloud.config.uri=${CONFIG_SERVICE}
management.security.enabled=false
spring.cloud.stream.bindings.inventory-out.group=picking-service-group
spring.cloud.stream.bindings.springCloudBusOutput.group=picking-service-group